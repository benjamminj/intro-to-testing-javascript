const avg = require("./avg");

describe("avg()", () => {
  it("should return the average of the numbers", () => {
    // uncomment to allow the test runner to run this test
    // const result = avg(1, 2, 3, 4, 5, 6);
    // expect(result).toEqual(3.5);
  });
});
